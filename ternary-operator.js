const loggedIn = true;

const greeting = `User is ${!loggedIn ? 'not ' : ''}logged in.`;

// if (loggedIn) {
//     greeting = 'User is logged in.';
// } else {
//     greeting = 'User is not logged in';
// }


console.log(greeting);